cmake_minimum_required(VERSION 2.8)

#compiler check
IF(CMAKE_COMPILER_IS_GNUCXX)
	EXECUTE_PROCESS(COMMAND ${CMAKE_C_COMPILER} -dumpversion OUTPUT_VARIABLE GCC_VERSION)
	
	IF(GCC_VERSION VERSION_LESS 4.8)
		IF(IGNORE_COMPILER_VERSION)
			MESSAGE(STATUS "Gorgon Library requires GCC 4.8+")
		ELSE()
			MESSAGE(STATUS "use cmake -DIGNORE_COMPILER_VERSION=1 to continue anyway")
			MESSAGE(FATAL_ERROR "Gorgon Library requires GCC 4.8+")
		ENDIF()
	ENDIF()
ELSEIF(MSVC)
	IF(MSVC_VERSION VERSION_LESS 11)
		IF(IGNORE_COMPILER_VERSION)
			MESSAGE(STATUS "Gorgon Library requires Visual Studio 2012+")
		ELSE()
			MESSAGE(STATUS "use cmake -DIGNORE_COMPILER_VERSION=1 to continue anyway")
			MESSAGE(FATAL_ERROR "Gorgon Library requires Visual Studio 2012+")
		ENDIF()
	ENDIF()
ELSE()
	IF(IGNORE_COMPILER_VENDOR)
		MESSAGE(STATUS "Unsupported compiler! Gorgon Library requires GCC 4.8+ OR Visual Studio 2012+")
		MESSAGE(STATUS "You may need to manually activate C++11 features")
	ELSE()
		MESSAGE(STATUS "use cmake -DIGNORE_COMPILER_VENDOR=1 to continue anyway")
		MESSAGE(FATAL_ERROR "Unsupported compiler! Gorgon Library requires GCC 4.8+ OR Visual Studio 2012+")
	ENDIF()
ENDIF()

#enable C++11 and 32-bit compilation
INCLUDE(CheckCXXCompilerFlag)
IF(CMAKE_COMPILER_IS_GNUCXX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -m32")
	SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -m32")
ENDIF()

#make sure 64bits cannot be activated
IF(MSVC)
	IF(${CMAKE_CL_64})
		MESSAGE(FATAL_ERROR "Gorgon Library works only 32bits")
	ENDIF()
ENDIF()