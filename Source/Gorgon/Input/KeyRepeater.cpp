#include "KeyRepeater.h"

namespace Gorgon { namespace Input {

    bool KeyRepeater::Progress(unsigned &leftover) {

        return true;
    }

    int KeyRepeater::GetDuration() const {
        return 0;
    }

} }
